# 项目记忆文档 - 披花沐雪

> 最后更新：2026-05-10 14:10
>
> 记忆文档位置：`docs/{更改主题}-{时间}.md`
>
> 主要文档：`README.md` | `project.md` | `docs/design.md` | `docs/DEPLOY.md`
>
> - README.md - 项目介绍、技术栈、快速开始、API文档
> - project.md - 开发计划、数据库设计、API接口、待完成项
> - docs/design.md - 枯木冷茶配色、CSS变量、响应式策略
> - docs/DEPLOY.md - 部署步骤、Nginx配置、Docker部署

## 项目管理规范（重要）

1. **每次代码修改必须生成记忆文档**，存放在 `docs/` 目录，格式：`{更改主题}-{时间}.md`
2. 记忆文档需包含：修改时间、修改内容列表、详细说明、关联文件
3. 同步更新 CONTEXT.md 的「最近修改」记录（保留最近 10 条）
4. 代码审查发现保留在 CONTEXT.md，修复后删除，只保留待办项
5. **修改 CONTEXT.md 结构前，先确认用户意图再动手**
6. **待执行** = 现在要干的；**暂时搁置** = 以后再干的
7. **代码审查结果先放入待执行**，让用户选择要修哪些，不要直接修
8. **待办项格式**：每项三行 - 名字、描述、影响
9. **修改代码后同步更新主要文档**（README.md、project.md 等）

## 最近修改

1. **2026-05-10 14:10** - 更新待办项：删除RSS、标签云、访客统计，保留搜索、SEO、暗色模式
2. **2026-05-10 14:00** - 同步文档：更新版本号1.0.2、更新日志、待完成项状态
3. **2026-05-10 13:45** - viewTracker 持久化：改用数据库存储浏览记录，支持重启后数据保留
4. **2026-05-10 13:30** - 代码审查修复：认证重构、定时器清理、IP处理、验证修复、统计API、提示文字、tags去重
5. **2026-05-10 12:00** - 代码审查修复：XSS防护、上传安全、输入验证、浏览量防刷
6. **2026-05-10 11:24** - 代码优化：Toast提示、速率限制、响应式模糊
7. **2026-05-10 11:16** - 添加 express-validator 输入验证

## 代码审查待办

### 暂时搁置

1. .env 清理
   - 部署前 `git rm --cached server/.env`
   - 确保敏感信息不提交到仓库

2. .env 生产配置
   - 部署时配置正式的 JWT_SECRET、DB_PATH、CORS_ORIGIN 等
   - 当前是开发环境配置，不安全

3. JWT 密钥加固
   - `config/index.js:11` 有 fallback 默认值 `'default-secret-key'`
   - 生产环境使用弱密钥，JWT 可被伪造

### 待执行

**新功能：**

1. 文章搜索功能
   - 全文搜索标题和内容
   - 支持关键词高亮显示

2. SEO 优化
   - 添加 meta 标签、Open Graph
   - 生成 sitemap.xml

3. 暗色模式
   - 切换明暗主题
   - 记住用户偏好
