# viewTracker 持久化 - 2026-05-10 13:45

## 修改时间
2026-05-10 13:45

## 修改内容

### 数据库变更
- 添加 `view_tracking` 表到 `schema.sql`
  - 字段：id, ip_address, post_id, viewed_at
  - 唯一约束：UNIQUE(ip_address, post_id)
  - 索引：ip_post 复合索引、viewed_at 索引

### 后端变更
- **移除内存 Map 存储** - `postController.js:6-14`
  - 移除 `viewTracker` Map 和 `setInterval` 清理逻辑
  - 移除 `viewCleanupTimer` 导出

- **添加数据库操作函数** - `postController.js:6-30`
  - `hasRecentlyViewed(db, ip, postId)` - 检查5分钟内是否浏览过
  - `recordView(db, ip, postId)` - 记录浏览
  - `cleanupOldViews(db)` - 清理24小时前的记录

- **修改浏览量逻辑** - `postController.js:105-115`
  - 使用数据库查询代替内存 Map
  - 1% 概率触发清理过期记录

- **简化 SIGINT 处理** - `app.js:65-69`
  - 移除 `clearInterval(viewCleanupTimer)`

### 优势
1. 服务器重启后浏览记录不丢失
2. 支持多实例部署共享数据
3. 内存占用更稳定

## 关联文件

- `server/db/schema.sql`
- `server/controllers/postController.js`
- `server/app.js`
