# 代码审查修复 - 2026-05-10 13:30

## 修改时间
2026-05-10 13:30

## 修改内容

### 阻塞问题（1-4项）：认证系统重构
1. **移除响应体中的 token** - `authController.js:44-52`
2. **移除 localStorage token** - `auth.js:6,9,14,33`
3. **移除 store 中的 token 存储** - `auth.js:37-44`
4. **移除请求拦截器中的 Authorization header** - `api/index.js:13-25`
   - 添加 `withCredentials: true` 支持 cookie 自动携带

### 重要问题（5-11项）

5. **清除 viewTracker 定时器** - `postController.js:9-14`, `app.js:65-69`
   - 保存 setInterval 引用，SIGINT 时清除

6. **处理 IPv6 格式的 IP** - `postController.js:8-11,105`
   - 添加 `normalizeIp()` 函数，移除 `::ffff:` 前缀

7. **avatar 空字符串验证** - `validator.js:102-108`
   - 添加空字符串检查，允许空值通过

8. **updateProfile 空字符串处理** - `authController.js:106-107`
   - 使用条件判断，空字符串时保留旧值

9. **Dashboard 统计不准确** - `postController.js`, `post.js`, `api/post.js`, `Dashboard.vue`
   - 添加后端统计 API `GET /api/posts/stats`
   - 前端使用专门的统计接口

10. **删除提示与后端不一致** - `Categories.vue:190`
    - 修改提示文字：如果该分类下有文章，需要先转移或删除文章

### 建议（12项）

11. **tags 处理去重** - `Editor.vue:219`
    - 使用 `Set` 去重后再 join

## 关联文件

- `server/controllers/authController.js`
- `server/controllers/postController.js`
- `server/routes/post.js`
- `server/middleware/validator.js`
- `server/app.js`
- `client/src/stores/auth.js`
- `client/src/api/index.js`
- `client/src/api/post.js`
- `client/src/views/admin/Dashboard.vue`
- `client/src/views/admin/Categories.vue`
- `client/src/views/admin/Editor.vue`
