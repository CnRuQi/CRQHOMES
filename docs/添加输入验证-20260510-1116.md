# 添加输入验证 (express-validator)

> 时间：2026-05-10 11:16

## 本次主要修改

1. 安装 express-validator 依赖
2. 创建 server/middleware/validator.js 验证中间件
3. 更新 post 路由添加验证规则
4. 更新 auth 路由添加验证规则
5. 更新 category 路由添加验证规则
6. 更新 CONTEXT.md 标记待办事项完成

## 详细说明

### 新增文件
- `server/middleware/validator.js` - 统一验证中间件

### 验证规则
| 路由 | 验证内容 |
|------|----------|
| POST /api/auth/login | username: 3-30字符, password: ≥6位 |
| POST /api/posts | title: 必填≤200, content: 必填 |
| PUT /api/posts/:id | id: 整数, title/content: 必填 |
| POST /api/categories | name: 必填≤50 |

### 代码变更
- `server/routes/post.js` - 添加 postRules 验证
- `server/routes/auth.js` - 添加 authRules 验证
- `server/routes/category.js` - 添加 categoryRules 验证
- `server/package.json` - 添加 express-validator 依赖

## 关联文件
- CONTEXT.md - 待办事项已更新
